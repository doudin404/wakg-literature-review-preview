interface:
  display_name: "WAKG 论文数据流水线"
  short_description: "低人工协同完成论文发现、MAT/MIX 提取与验收"
  default_prompt: "Use $wakg-literature-pipeline to discover, extract, and validate one WAKG literature record."
policy:
  allow_implicit_invocation: true
